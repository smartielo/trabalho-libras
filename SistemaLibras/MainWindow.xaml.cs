using System.Windows;

// Certifique-se de que "SistemaLibras" é o nome exato do seu projeto.
// Se você deu outro nome, ajuste a linha abaixo.
namespace SistemaLibras
{
    /// <summary>
    /// Interaction logic for MainWindow.xaml
    /// </summary>
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            // Este método conecta o seu código C# ao design feito no arquivo MainWindow.xaml.
            // O erro aqui deve desaparecer assim que os outros erros de sintaxe forem corrigidos.
            InitializeComponent();
        }

        // Os métodos para os cliques dos botões virão aqui no futuro.
    }
}